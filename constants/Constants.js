const constants = {
    GET: "GET",
    POST: "POST",
    UPDATE: "PUT",
    DELETE: "DELETE"
}

const hosts = {
    JSONPlaceHolder: "http://jsonplaceholder.typicode.com"
}

const paths = {
    Todos: "todos",
    Posts: "posts",
    Comments: "commnets",
    Albums: "albums",
    Photos: "photos",
    Users: "users"
}

const searchBy = {
    User: "userId"
}

export {constants, hosts, paths, searchBy};